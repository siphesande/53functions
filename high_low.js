













var arr = [100, 0, 50];
var list=[];
function high_low() {
arr.min(); // return 0
arr.max(); 












/*function number_list(n) {
	var list = [];//list array
	for(var i = 1; i <= n; i++) {
		console.log(i);
		//The push() method adds new items to the end of an array(list), and returns the new length.
		list.push(i);
	}
	return list.max();
	       list.min();
	   }





  








//The following function uses Function.prototype.apply() to find the maximum element
// in a numeric array. getMaxOfArray([1, 2, 3]) is equivalent
// to Math.max(1, 2, 3), but you can use getMaxOfArray() on programmatically constructed arrays of any size.

//function getMaxOfArray(numArray) {
  //return Math.max.apply(null, numArray);
//